import React from "react";
import { clsx } from "clsx";
import { Card } from "../../atoms/Container";
const Container = (props) => {
  return <Card className="nes-container" {...props}></Card>;
};

export default Container;
