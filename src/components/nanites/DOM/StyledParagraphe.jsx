import { styled } from "styled-components";

const StyledParagraphe = styled.p`
  color: ${(props) => props.theme.colors.primary};
  font-family: "Press Start 2P";
  font-size: 0.6em;
`;

export default StyledParagraphe;
